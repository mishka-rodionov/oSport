package com.rodionov.osport.app.di.module

import android.content.Context
import org.koin.dsl.module

val roomModule = module {

//    single { provideDatabase(get()) }
//    single { getRequestDraftDao(get()) }
}

//fun provideDatabase(context: Context) = OrgHimDatabase.buildDataSource(context)
//
//fun getRequestDraftDao(orgHimDatabase: OrgHimDatabase) = orgHimDatabase.requestDraftDao()

